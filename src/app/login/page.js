// Importamos el componente de esa direccion llamada LoginForm
import LoginForm from "../../components/LoginForm";

// Creamos un componente de react exportado para ser utilizado en la pagina principal 
export default function LoginPage() {
    return <LoginForm />;
}